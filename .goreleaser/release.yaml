version: 1

builds:
  - skip: true

changelog:
  sort: asc
  filters:
    exclude:
      - "^chore:"
      - "^docs:"
      - "^test:"

release:
  prerelease: auto
  extra_files:
    - glob: ./jqless-build/**/*
    - glob: ./jqless-build/checksums.txt